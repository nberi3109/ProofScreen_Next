import Link from "next/link";
import { ArrowUpRight, MoreHorizontal, Users } from "lucide-react";
import { RecruiterJob } from "@/types/recruiter/job";
export default function RecruiterJobTable({ jobs }: { jobs: RecruiterJob[] }) { return <div className="recruiter-table-wrap"><table className="recruiter-table"><thead><tr><th>Job</th><th>Status</th><th>Applicants</th><th>Qualified</th><th>Posted</th><th /></tr></thead><tbody>{jobs.map(job=><tr key={job.id}><td><Link href={`/recruiter/jobs/${job.id}`} className="table-job"><span className="job-table-icon">{job.title.charAt(0)}</span><span><b>{job.title}</b><small>{job.department} · {job.location}</small></span></Link></td><td><span className={`status-pill ${job.status.toLowerCase()}`}>{job.status}</span></td><td><Link href={`/recruiter/jobs/${job.id}/applicants`} className="table-number"><Users size={14}/>{job.applicants}</Link></td><td><b>{job.qualified}</b></td><td className="muted-cell">{job.posted}</td><td><Link href={`/recruiter/jobs/${job.id}`} className="table-action" aria-label={`Open ${job.title}`}><ArrowUpRight size={16}/></Link><button className="table-action" aria-label="More options"><MoreHorizontal size={16}/></button></td></tr>)}</tbody></table></div> }
